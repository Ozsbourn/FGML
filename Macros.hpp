#ifndef FGML_MACROS_HPP_
#define FGML_MACROS_HPP_

namespace FGML {
	///
	///	Regular macros
	///

	#define SQR(A) ((A) * (A))

	#define MAX(A, B) (((A) > (B)) ? (A) : (B))
	#define MIN(A, B) (((A) < (B)) ? (A) : (B))

	///
	///	Regular macros end
	///
};

#endif // FGML_MACROS_HPP_